<div class="form-group">
    <label><strong>{{ $textarea_label }}</strong></label>
    <textarea id="{{ $textarea_id }}" name="{{ $textarea_name }}" class="form-control wysiwyg_editor" rows="{{ $textarea_rows }}" placeholder="{{ $textarea_placeholder }}">{{ $textarea_value }}</textarea>
</div>
