<div class="form-group">
    <label><strong>{{ $select_label }}</strong></label>
    <select name="{{ $select_name }}" id="{{ $select_id }}" class="form-control">
        {{ $select_options }}
    </select>
</div>
